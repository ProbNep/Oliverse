import { defineConfig } from "@lovable.dev/vite-tanstack-config";

export default defineConfig({
  // We leave this empty because Lovable's config package automatically
  // handles the plugins under the hood without breaking the compiler.
});